# 复制本文件为仓库根目录的 .env，填入真实值。.env 已在 .gitignore，不会被提交。
# 不要把钥匙贴到聊天里。写好后告诉我「.env 已放好」即可。

# GitHub fine-grained token。只授权仓库 eltonq3/fe-guide-wanlvqiansi，权限 Contents: Read and write。
GITHUB_TOKEN=

# Cloudflare API Token。权限用 Account → Cloudflare Pages → Edit；新界面里的名称是 Pages Write。
CLOUDFLARE_API_TOKEN=

# Cloudflare 控制台右侧的 Account ID。与 tools/deploy.sh 历史注释核对是不是同一个账号。
CLOUDFLARE_ACCOUNT_ID=
